$(function () {
  initCarousel();
  initDropDown();
  initDatepicker();
});

function initCarousel() {
  $(window).on('resize', () => {});
}

function initDropDown() {
  $(window).on('resize', () => {});
}

function initDatepicker() {
  $(window).trigger('resize');
}
